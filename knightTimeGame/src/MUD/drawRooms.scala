package MUDGame

class drawRooms {
  def run(currentRoom: Int, ID: Int) {

    /*test.rooms(0).exits.foreach(e => println(e.destination+" "+e.exitDir))
    println()
    test.rooms(1).exits.foreach(e => println(e.destination+" "+e.exitDir))
    println()
    test.rooms(2).exits.foreach(e => println(e.destination+" "+e.exitDir))
    println()
    test.rooms(3).exits.foreach(e => println(e.destination+" "+e.exitDir))*/
  }
}